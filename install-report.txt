install one-liner test
commit: 487c643222a15e7eb74a1d0981206094419c0c93
date:   2026-07-28 05:31:40 UTC

command under test:
  curl -fsSL https://raw.githubusercontent.com/Devehab/linko/main/install.sh | bash

hosts:      success
containers: failure

════════════════════ per platform ════════════════════

### PASS ──────── alpine:latest ────────
    ──────── alpine:latest ────────
    uname: Linux x86_64
    shipped with:
      sh    /bin/sh
      bash  (absent)
      curl  (absent)
      tar   /bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failure writing output to destination, passed 1370 returned 0
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── archlinux:latest ────────
    ──────── archlinux:latest ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/sbin/sh
      bash  /usr/sbin/bash
      curl  /usr/sbin/curl
      tar   /usr/sbin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: Failed writing body
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── debian:11 ────────
    ──────── debian:11 ────────
    uname: Linux x86_64
    shipped with:
      sh    /bin/sh
      bash  /bin/bash
      curl  (absent)
      tar   /bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failure writing output to destination
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── debian:12 ────────
    ──────── debian:12 ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/bin/sh
      bash  /usr/bin/bash
      curl  (absent)
      tar   /usr/bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failed writing body
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── fedora:latest ────────
    ──────── fedora:latest ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/sbin/sh
      bash  /usr/sbin/bash
      curl  /usr/sbin/curl
      tar   /usr/sbin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failure writing output to destination, passed 1370 returned 653
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── macos-latest ────────
    ──────── macos-latest ────────
    uname: Darwin arm64
      curl  /usr/bin/curl
      tar   /usr/bin/tar
      bash  /bin/bash
      go    (absent)
    
    $ curl -fsSL .../install.sh | bash
    [2mInstalling linko v0.2.3 (darwin_arm64)[0m
    [32m✓ linko installed to /opt/homebrew/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /opt/homebrew/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)
    
    Error: linko is not set up yet — run `linko init` first
    
    [exit 0]

### PASS ──────── opensuse/leap:latest ────────
    ──────── opensuse/leap:latest ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/bin/sh
      bash  /usr/bin/bash
      curl  /usr/bin/curl
      tar   /usr/bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failure writing output to destination, passed 1370 returned 1089
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── rockylinux/rockylinux:9 ────────
    ──────── rockylinux/rockylinux:9 ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/bin/sh
      bash  /usr/bin/bash
      curl  /usr/bin/curl
      tar   /usr/bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:

### PASS ──────── ubuntu:22.04 ────────
    ──────── ubuntu:22.04 ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/bin/sh
      bash  /usr/bin/bash
      curl  (absent)
      tar   /usr/bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    curl: (23) Failure writing output to destination
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── ubuntu:24.04 ────────
    ──────── ubuntu:24.04 ────────
    uname: Linux x86_64
    shipped with:
      sh    /usr/bin/sh
      bash  /usr/bin/bash
      curl  (absent)
      tar   /usr/bin/tar
      go    (absent)
    
    adding bash/curl/tar/ca-certificates only:
      go is still absent — good
    
    $ curl -fsSL .../install.sh | bash
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)

### PASS ──────── ubuntu-latest ────────
    ──────── ubuntu-latest ────────
    uname: Linux x86_64
      curl  /usr/bin/curl
      tar   /usr/bin/tar
      bash  /usr/bin/bash
      go    /usr/bin/go
    
    $ curl -fsSL .../install.sh | bash
    [2mInstalling linko v0.2.3 (linux_amd64)[0m
    [32m✓ linko installed to /usr/local/bin/linko[0m
    
    Next step:
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /usr/local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)
    
    Error: linko is not set up yet — run `linko init` first
    
    [exit 0]

### PASS ──────── windows-latest ────────
    ──────── windows-latest ────────
    uname: MINGW64_NT-10.0-26100 x86_64
      curl  /mingw64/bin/curl
      tar   /usr/bin/tar
      bash  /usr/bin/bash
      go    /c/hostedtoolcache/windows/go/1.24.13/x64/bin/go
    
    $ curl -fsSL .../install.sh | bash
    [2mInstalling linko v0.2.3 (windows_amd64)[0m
    [32m✓ linko installed to /c/Users/runneradmin/.local/bin/linko.exe[0m
    
    [2mcreated .bashrc with /c/Users/runneradmin/.local/bin on PATH[0m
    
    Next steps:
      exec $SHELL       # reload your shell once
      linko init
    
    Before that you need a Cloudflare API token with BOTH permissions:
      Zone     ->  DNS                ->  Edit
      Account  ->  Cloudflare Tunnel  ->  Edit
      Create it at https://dash.cloudflare.com/profile/api-tokens
    
    Full guide (install, token, commands, troubleshooting):
    [32m  https://github.com/Devehab/linko/blob/main/GUIDE.md[0m
      or run:  linko docs
    [install exit 0]
    
    $ command -v linko
    /c/Users/runneradmin/.local/bin/linko
    [exit 0]
    $ linko --version
    linko 0.2.3
    [exit 0]
    $ linko --help
    [exit 0]
    $ linko docs
    [exit 0]
    $ linko list  (must steer an unconfigured user to init)
    
    Error: linko is not set up yet — run `linko init` first
    
    [exit 0]
