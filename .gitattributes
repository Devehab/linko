# Keep LF in the working tree everywhere, including Windows checkouts.
#
# Without this, git converts Go sources to CRLF on Windows and `gofmt -s -l .`
# then reports every file in the repository as needing formatting.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.gz  binary
*.zip binary
